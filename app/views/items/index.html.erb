<div class="Items"> 
  <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.price_publix %></td>
        <td><%= link_to image_tag("#{item.picture}"), item %></td>
      </tr>
        

       
      
  <% end %>
</div><!-- /.blog-post -->

