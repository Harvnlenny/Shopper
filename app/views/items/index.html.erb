<div class="Items"> 
  <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= format_money(item.price_publix) %></td>
        <td><%= link_to image_tag("#{item.picture.thumb}"), item %></td>
      </tr>
        

       
      
  <% end %>
</div><!-- /.blog-post -->

